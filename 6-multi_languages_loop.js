let langLoop = ["C is fun", "Python is cool", "JavaScript is amazing"];
let i = 0;  // index initializing

while(i < langLoop.length){   // stoping condition
  console.log(langLoop[i]);   // printing the output
  i++;                        // incrementing by one per each iteration
}